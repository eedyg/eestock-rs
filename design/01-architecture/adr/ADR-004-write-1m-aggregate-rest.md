# ADR-004：只写 1m，高周期连续聚合

- 状态：✅ 已定
- 决策：采集只落 **1m bar**（最小抓取间隔 1min，可配置放大）；5m/15m/1h/日线全部由 TimescaleDB **连续聚合**自动生成；单一事实源
- 筹码分布的日线输入同样来自聚合（ADR-011）
- 例外预留：若未来需要 >8 天的 1m 历史，唯一途径是从现在起累积（免费源物理上限：腾讯≈2天/新浪≈8天）
