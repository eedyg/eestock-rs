[package]
name = "app"
description = "二进制装配：DI 组装、配置加载、进程入口（src 由 design/03-collector/02-data-plane.md 与 design/07-app-plane/00-web-api.md tangle 生成）"
version.workspace = true
edition.workspace = true

[dependencies]
domain = { path = "../domain" }
collector = { path = "../collector" }
providers = { path = "../providers" }
storage = { path = "../storage" }
tushare = { path = "../tushare" }
diagnose = { path = "../diagnose" }
alert = { path = "../alert" }   # Wave 2 Phase B：告警引擎（eestock-app 装配 AlertService + 评估节拍）
web = { path = "../web" }
mcp = { path = "../mcp" }   # Wave 1 Phase D：MCP HTTP/SSE（ADR-009，与 web 同进程、端口独立 8082）
application = { path = "../application" }   # Wave 3 Phase 3c：回测 BacktestService（app bin DI）
axum.workspace = true
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
serde.workspace = true
toml.workspace = true
anyhow.workspace = true
chrono.workspace = true
sqlx.workspace = true

[dev-dependencies]
tokio.workspace = true
